name: E2E (ratemate)

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]

jobs:
  e2e:
    uses: ./.github/workflows/e2e-common.yml
    with:
      site: ratemate
      base_url: https://store.ratemate.top
      login_path: /login
      register_path: /login
      browsers: "chromium"
      run_login_success: false
    secrets: inherit
